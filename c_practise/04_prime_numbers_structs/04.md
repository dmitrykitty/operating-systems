We can define structure by following syntax: 
```c
struct S {
    int x, y;
} 
```
and in main we create this struct `struct S t = {1, 2};`
what woulb be &t? It is pointer to first element of the struct? How is it implemented? 
I can create pointer to this struct `struct S* ps = &s` but what exactly ps mean? And noe I can access memebers of the struct using two options: 
```c
(*ps).x = 5; 
//or 
ps->x = 5; 
```
so operator `->` is just syntax sugar for first option. 